# 1. Stage all changed files
git add src/components/tabs/IntelligenceTab.jsx
git add src/components/tabs/PremarketTab.jsx
git add src/components/tabs/ScannerTab.jsx
git add src/components/tabs/RotationTab.jsx
git add src/components/tabs/SectorsTab.jsx
git add src/components/tabs/ThemesTab.jsx

# 2. Commit with a detailed message
git commit -m "Fix: Resolve all T is not defined and MultiSelectDropdown parameter conflicts

- Moved helper components inside IntelligenceTab to access T via closure
- Fixed destructuring default in RiskCalculator (color = T.text)
- Replaced top-level heat functions with CSS variables in Rotation/Sectors/Themes
- Renamed _color parameter to propColor in MultiSelectDropdown across all tabs to avoid redeclaration
- All tabs now render without runtime errors"

# 3. Push to GitHub (replace YOUR_USERNAME and YOUR_TOKEN if needed)
git push -u origin main


git remote -v

git remote add origin git@github.com:yourherohere/market-dashboard.git

git push -u origin main
git push origin main --force

### Claude

git add -A
git commit -m "All fix done by Deepseek by all tab"
git push origin main


Note: Scan
1. Recent High Broke
2. VCP
3. HH-HL
4. 1 or 2 tight candle and break 

