# ── Server ───────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV=development
LOG_LEVEL=info

# ── Database ─────────────────────────────────────────────────────────────────
# DB_PATH=./data/market.db

# ── Yahoo Finance ─────────────────────────────────────────────────────────────
YF_DISABLE_VERSION_CHECK=1

# ── Frontend (Vite) ───────────────────────────────────────────────────────────
# During development, Vite proxies /api/* to localhost:3001 automatically.
# Only set VITE_API_BASE_URL when deploying to a remote server:
# VITE_API_BASE_URL=https://api.yourdomain.com

# Comma-separated list of allowed CORS origins (production)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
